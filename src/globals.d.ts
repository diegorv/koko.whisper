// Compile-time constants injected by `vite.config.js` via the
// `define` block. Declared as ambient globals so TypeScript / Svelte
// files can read them without an explicit import.

declare const __BUILD_INFO__: string;
